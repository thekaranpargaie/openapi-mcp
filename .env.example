# OpenAPI Specification URL
# Where to load your OpenAPI spec from (URL or file path)
OPENAPI_SPEC_URL=http://localhost:8000/openapi.json

# API Base URL
# Where the actual API is hosted (for making tool calls)
API_BASE_URL=http://localhost:8000

# Optional API Authentication
# Bearer token for authenticating with your API
API_KEY=your_api_key_if_needed

# Transport Mode
# Options: 'http' (default) or 'stdio'
# - http: For remote clients, Cloudflare AI Playground
# - stdio: For local integrations (Claude Desktop, subprocess)
TRANSPORT_MODE=http

# HTTP Server Port
# Only used when TRANSPORT_MODE=http
PORT=3000

# DNS-Rebinding Protection
# Comma-separated list of allowed origins (optional)
# Leave empty to allow all origins (suitable for dev)
# Example: https://playground.ai.cloudflare.com,https://your-domain.com
ALLOWED_ORIGINS=
